---
title: "Market Data"
description: "Live snapshot of one market, safe to poll at high frequency: last price, per-window volume, liquidity, market cap, supply, buys/sells and unique traders per window (5m/1h/24h), buy/sell tax."
---

`GET /v1/pool/data` · **1 credit**

Live snapshot of one market, safe to poll at high frequency: last price, per-window volume, liquidity, market cap, supply, buys/sells and unique traders per window (5m/1h/24h), buy/sell tax.

<ParamField query="chain" type="string" required>
  Public chain id - `evm:<id>` or `solana`. Example: `evm:8453`
</ParamField>

<ParamField query="address" type="string" required>
  Pool / pair address (the stable market id on its chain - Uniswap V4 poolIds supported). Example: `0x0ca6485b7e9cf814a3fd09d81672b07323535b64`
</ParamField>

<ParamField query="windows" type="string">
  Comma-separated stat windows. Example: `5m,1h,24h`
</ParamField>

<Note>Rolling out now - being finalized with our first design partners. Ping us for early access.</Note>

